<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<% if user_signed_in? %> 
    <span style="margin-left:2.5em"><%= link_to 'Edit', edit_project_path(@project) %></span>
    <span style="margin-left:2.5em"><%= link_to 'Back', projects_path %></span>
 <% else %>
    <%= link_to 'Back', projects_path %>
  <% end %>




